# Model Artifacts

`ml_model.pkl` is generated by:

```bash
python backend/scripts/train_model.py
```

The saved bundle contains:

- `model`: best trained regressor
- `model_name`: selected model name
- `metrics`: RMSE, MAE, and R2 for the selected model
- `feature_columns`: prediction feature order
